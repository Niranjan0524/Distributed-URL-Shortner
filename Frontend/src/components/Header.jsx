const Header=() => {

    //This component contains the app name in left corner  and in right side signup/login(if not logged in) or profile/logout(if logged in)

    return <>(
        <div className="header bg-blue-500 text-white p-4 text-center">
            <h1>URL Shortener</h1>
        </div>
    )

    </>

}

export default Header;